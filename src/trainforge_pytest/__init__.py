"""pytest plugin: scenarios as pytest cases.

See :mod:`trainforge_pytest.plugin` for the plugin implementation.

Install via:

    pip install "trainforge[pytest]"

Then point your project at a folder of TrainForge scenarios and run
``pytest``. Each scenario becomes a parametrized pytest case.
"""
